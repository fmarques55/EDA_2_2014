#include <stdio.h>
#include <stdlib.h>

int includeToArray(int *array, int valueToAdd, int arrayCapacity, int arrayLenght)
{


    if(arrayLenght <= arrayCapacity)
    {
        for(i=0;i<arrayCapacity;i++)
        {
            if(array[i]==0)
            {
                array[i]=
            }
        }
    }
}
int main()
{
    int array[10];

    for(i=0;i<10;i++)
    {
        array[i]=0;
    }


}
