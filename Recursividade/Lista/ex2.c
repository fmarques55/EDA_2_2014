#include <stdio.h>
#include <stdlib.h>

int stringSearch(char *string, char character)
{
	if()
}
int main()
{
	char string[20],characterForSearch;
	
	printf("Entre com a string: ");
	scanf("%s", string);
	
	printf("Entre com o caractere que deseja procurar na string: ");
	scanf("%c", &characterForSearch);

	return 0;	
}
